const generateInvoice = require("../../utils/generateInvoice");

exports.createInvoice = async (req, res, next) => {
  try {
    // Generate Invoice Save pdf in my Public/invoice
    // send pdf send as response
  } catch (error) {}
};
